#include<stdio.h>
#include<stdlib.h>
#include "library.h"
typedef struct tree
{  
int data;
struct tree *left,*right;
int ht;
}tree;


tree * LL(tree *T)
{
               T=rotateright(T);
               return(T);
}
